# regenerable: `bake/00_fetch_annotations.py` re-downloads, the rest re-derives
/data/
# the vendored GPL upstream clone carries its own .git
/upstream/
# toolchains
.venv/
node_modules/
__pycache__/
*.pyc
# build output
dist/
web/dist/
.DS_Store

# playwright
test-results/
playwright-report/

# art pipeline: downloaded sources and intermediate bakes, all regenerable (art/fly/build.sh)
art/.cache/
